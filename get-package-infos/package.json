{
  "name": "get-package-infos",
  "version": "1.0.0",
  "description": "returns the current version, clean semver version and name from package.json",
  "author": "Valentin D. Pinkman <valentin.d.pinkman@icloud.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "ncc build main.js -o build -m",
    "watch": "yarn build --watch"
  },
  "dependencies": {
    "@actions/core": "^1.2.3",
    "@actions/github": "^2.1.1",
    "@zeit/ncc": "^0.21.1",
    "semver": "^7.1.3"
  }
}
